<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<script is:inline src="/scripts/logo.js" defer></script>
		<script is:inline src="/scripts/nav.js" defer></script>
    <script is:inline src="/scripts/usuario.js" defer></script>
    <script is:inline src="/scripts/carrito.js" defer></script>

		<title>LowPádel</title>
	</head>
	<body>
    <div id="top"></div>
		<div id="loader">
  			<img src="/Layout/logo.png" alt="Logo">
		</div>

    <video autoplay muted loop  id="fondo-video">
      <source src="/videos/videoplayback.mp4" type="video/mp4">
      Tu navegador no soporta video de fondo.
    </video>

    <div id="overlay"></div>

		<header>

			<a href="/" class="cabecera">
				<img src="/Layout/logo.png" alt="Logo">
			</a>

			<div class="header-actions">
				<a href="/carrito" class="carrito-link" title="Mi Carrito">
					<svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
						<circle cx="9" cy="21" r="1"></circle>
						<circle cx="20" cy="21" r="1"></circle>
						<path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"></path>
					</svg>
					<span id="carrito-count" class="carrito-badge">0</span>
				</a>

				<div class="sesion">
					<img id="imagenPerfil" src="https://images.icon-icons.com/1378/PNG/512/avatardefault_92824.png" alt="Foto de perfil" style="
					border-radius:50%; 
					width:90px; 
					height:90px; 
					object-fit:cover; 
					border:4px solid var(--primary);
					margin-bottom: 5px;
					"><br>
					<a href="/register" class="registro">
						Cuenta
					</a>
				</div>
			</div>
      
		</header>
		<nav>
			<ul>
				<li><a href="/palas">Nuestras Palas</a></li>
				<li><a href="/noticias">Noticias</a></li>
				<li><a href="/contact">Contacto</a></li>
				<li><a href="/about">Sobre nosotros</a></li>
			</ul>
		</nav>
				
		
		
		<slot />

<br><br><br>
		<footer>
    		<p>© 2024 LowPadel - Tu tienda especializada en pádel de élite | Envíos a toda España | <a href="/contact" style="color: #6ee7b7; text-decoration: none;">Contacto</a></p>
  		</footer>


    <a href="#top" id="backToTop">↑</a>



	</body>
</html>

<style>
	/* Paleta moderna verde/morado */
:root {
  --primary-dark: #1a2332;
  --primary: #00c896;
  --primary-light: #6ee7b7;
  --accent: #9333ea;
  --accent-light: #c084fc;
  --background: #0f172a;
  --surface: #1e293b;
  --text-light: #f1f5f9;
  --blanco: #ffffff;
  --color-principal: #00c896;
  --color-acento: #9333ea;
  --color-fondo-claro: rgba(241, 245, 249, 0.733);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html{
	background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
}

@keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

#loader {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--color-principal) 0%, var(--color-acento) 100%);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
    opacity: 1;
    visibility: visible;
    transition: opacity 0.8s ease, visibility 0.8s ease;
  }

  /* Logo */
#loader img {
	width: 600px;
	max-width: 90vw;
	animation: pulse 2s ease-in-out infinite;
}

@keyframes pulse {
	0%, 100% { transform: scale(1); }
	50% { transform: scale(1.05); }
}

  /* Cuando se oculta */
#loader.hidden {
	opacity: 0;
	visibility: hidden;
	pointer-events: none;
}

#fondo-video {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover; /* ajusta el video para cubrir toda la pantalla */
  z-index: -2; /* lo pone detrás de todo */
  pointer-events: none; /* para que no interfiera con clics */
  filter: saturate(0.8) brightness(0.8);
}

#overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    135deg,
    rgba(0, 200, 150, 0.2),
    rgba(147, 51, 234, 0.3)
  );
  z-index: -1;
  backdrop-filter: blur(3px) saturate(1.2);
}


body {
  font-family: 'Segoe UI', sans-serif;  
}

main, header, nav, footer, #backToTop, .sesion, .cabecera {
  position: relative;
  z-index: 1; /* Todo el contenido por encima del overlay */
}

header {
  background: linear-gradient(135deg, var(--color-principal) 0%, var(--color-acento) 100%);;
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  color: var(--text-light);
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 2rem;
  position: relative;
  box-shadow: 0 10px 30px rgba(0, 200, 150, 0.3);
}

.cabecera {
  flex: 1;
  text-align: center;
}

.cabecera img {
  width: 300px;
  margin-bottom: 10px;
}

nav{
	background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
	padding: 15px 10px;
	box-shadow: 0 4px 15px rgba(0, 200, 150, 0.3);
}

nav ul {
  list-style: none;
  display: flex;
  gap: 5rem;
  justify-content: center;
  font-size: 18pt;
}

nav a {
  color: var(--blanco);
  text-decoration: none;
  font-weight: bold;
}

nav a:hover {
  text-decoration: none;
  transform: translateY(-2px);
  text-shadow: 0 0 10px rgba(255, 255, 255, 0.8);
}

.header-actions {
  display: flex;
  align-items: center;
  gap: 2rem;
}

.carrito-link {
  position: relative;
  color: var(--text-light);
  transition: all 0.3s ease;
  display: flex;
  align-items: center;
}

.carrito-link:hover {
  color: var(--primary-light);
  transform: translateY(-2px);
}

.carrito-badge {
  position: absolute;
  top: -8px;
  right: -8px;
  background: linear-gradient(135deg, #9333ea, #c084fc);
  color: white;
  border-radius: 50%;
  width: 20px;
  height: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: bold;
  box-shadow: 0 2px 8px rgba(147, 51, 234, 0.5);
}

.sesion {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.sesion a {
  text-decoration: none;
  color: white;
  font-weight: bold;
  padding: 12px 24px;
  border-radius: 30px;
  transition: all 0.3s ease;
  cursor: pointer;
  box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
}

/* Registrarse */
.registro {
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
}

.registro:hover {
  background: linear-gradient(135deg, var(--primary-light) 0%, var(--primary) 100%);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0, 200, 150, 0.4);
}

/* Iniciar Sesión */
.inicioSesion {
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-light) 100%);
}

.inicioSesion:hover {
  background: linear-gradient(135deg, var(--accent-light) 0%, var(--accent) 100%);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(147, 51, 234, 0.4);
}


footer {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  background: linear-gradient(135deg, var(--primary-dark) 0%, var(--background) 100%);
  backdrop-filter: blur(10px);
  border-top: 1px solid rgba(0, 200, 150, 0.2);
  color: var(--text-light);
  text-align: center;
  padding: 1rem;
  box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.3);
}

#backToTop {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
  color: var(--blanco);
  font-size: 2rem;
  width: 55px;
  height: 55px;
  display: flex;
  justify-content: center;
  align-items: center;
  border-radius: 50%;
  cursor: pointer;
  box-shadow: 0 8px 20px rgba(0, 200, 150, 0.4);
  transition: all 0.3s ease;
  z-index: 10000;
  border: 2px solid rgba(255, 255, 255, 0.2);
}

#backToTop:hover {
  background: linear-gradient(135deg, var(--primary-light) 0%, var(--accent-light) 100%);
  transform: translateY(-5px);
  box-shadow: 0 12px 30px rgba(0, 200, 150, 0.5);
}
 

</style>
